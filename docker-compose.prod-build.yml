# Override para buildear el frontend localmente en el servidor
# Uso: docker-compose -f docker-compose.prod.yml -f docker-compose.prod-build.yml up --build -d frontend
services:
  frontend:
    image: ngcourses-frontend:local
    build:
      context: ./frontend
